import React from "react";
import  DataBody from "./DataBody/DataBody";
//import stylesheet

function DataTable ({users, headings, handleSort }) {
    
}

export default DataTable;

// table-hover

// table-striped

// // The map() method creates a new array populated with the 
// results of calling a provided function on every element in 
// the calling array.

// want